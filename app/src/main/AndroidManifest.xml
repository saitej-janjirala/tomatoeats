<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.saitejajanjirala.tomatoeats">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="false"
        android:debuggable="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="HardcodedDebugMode">
        <activity android:name=".activities.EnterotpandResetpassword"></activity>
        <activity android:name=".activities.Enterdetailsforotp" />
        <activity android:name=".activities.Placeordersuccessful" />
        <activity
            android:name=".activities.Cart"
            android:theme="@style/signup" />
        <activity
            android:name=".activities.Restaurantitems"
            android:theme="@style/signup" />
        <activity android:name=".activities.ContentsActivity" />
        <activity
            android:name=".activities.SignupActivity"
            android:theme="@style/signup" />
        <activity android:name=".activities.Loginactivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>